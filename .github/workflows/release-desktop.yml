name: Release desktop

#on:
#  push:
#    tags:
#      - 'v*'  # This will trigger the workflow on push to tags like v1.0, v2.0, etc.

on:
  push:
    branches:
      - "master"

jobs:
  build-windows:
    name: Build Windows binaries
    runs-on: windows-latest
    environment: production
    defaults:
      run:
        working-directory: 'frontend'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '16'

      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          default: true
          override: true

      - name: Install Dependencies
        run: npm ci

      - name: Build Tauri App
        run: npm run tauri-build

      - name: Upload Windows portable exe
        uses: actions/upload-artifact@v4
        with:
          name: exe
          path: ./frontend/src-tauri/target/release/*.exe

      - name: Upload Windows installer
        uses: actions/upload-artifact@v4
        with:
          name: exe
          path: |
            ./frontend/src-tauri/target/release/bundle/msi/*.exe
            ./frontend/src-tauri/target/release/bundle/nsis/*.exe
          

#  deployement:
#    needs: build
#    runs-on: ubuntu-latest
#    steps:
#      - name: Download Artifact
#        uses: actions/download-artifact@v3
#        with:
#          name: tauri-app-exe
#
#      - name: Create Release
#        id: create_release
#        uses: actions/create-release@v1
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        with:
#          tag_name: ${{ github.ref }}
#          release_name: Release ${{ github.ref }}
#          draft: false
#          prerelease: false
#
#      - name: Upload Release Asset
#        uses: actions/upload-release-asset@v1
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        with:
#          upload_url: ${{ steps.create_release.outputs.upload_url }}
#          asset_path: ./tauri-app-exe/*.exe  # Adjust the path if needed
#          asset_name: BetterFleet.exe  # Rename this to your app's name
#          asset_content_type: application/octet-stream
