name: Build and Release Tauri App

on:
  push:
    tags:
      - 'v*'  # This will trigger the workflow on push to tags like v1.0, v2.0, etc.

jobs:
  build:
    name: Build Tauri Application
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          default: true
          override: true

      - name: Install Dependencies
        run: yarn install  # Or npm install

      - name: Build Tauri App
        run: yarn tauri build  # Or npm run tauri build

      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: tauri-app-exe
          path: ./src-tauri/target/release/*.exe  # Adjust the path to your .exe location

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifact
        uses: actions/download-artifact@v3
        with:
          name: tauri-app-exe

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./tauri-app-exe/*.exe  # Adjust the path if needed
          asset_name: YourAppName.exe  # Rename this to your app's name
          asset_content_type: application/octet-stream
